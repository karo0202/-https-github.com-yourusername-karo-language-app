import React, { useState } from 'react';
import { Video, Calendar, Clock, Star, User, MessageCircle, Phone, Globe } from 'lucide-react';
import { motion } from 'framer-motion';

function LiveTutoring() {
  return (
    <div className="p-4">
      <h1 className="text-2xl font-bold">Live Tutoring</h1>
      <p>Connect with professional tutors for one-on-one sessions.</p>
    </div>
  );
}

export default LiveTutoring;